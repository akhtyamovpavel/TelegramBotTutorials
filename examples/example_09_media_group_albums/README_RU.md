# Example 9: Media Group Albums (Множественная загрузка изображений)

## ⚠️ Проблема дублирования и её решение

**Важно:** При обработке альбомов от пользователей возникает проблема **дублирования ответов** (бот отвечает несколько раз на один альбом).

**Решение:** Используйте **Middleware** для группировки сообщений!

📖 **[Подробное объяснение и решение → MIDDLEWARE.md](./MIDDLEWARE.md)**

Файлы с решением:
- **aiogram:** `bot_with_middleware.py` + `album_middleware.py`
- **python-telegram-bot:** `bot_with_middleware.py` + `album_middleware.py`

---

## 🎯 Что нового в этом примере

В предыдущих примерах мы научились:
- Отправлять одиночные изображения (Example 8)
- Получать файлы от пользователей (Example 7)

**Новые концепции:**
- **Media Group (Альбомы)** - отправка нескольких изображений/видео одним сообщением
- **Обработка альбомов от пользователя** - получение группы файлов как единого сообщения
- **MediaGroupBuilder** (aiogram) / InputMedia* (PTB) - построение альбомов
- **Album Middleware** ⭐ - правильная группировка медиа-файлов от пользователя (БЕЗ дублирования!)

## 📚 Концепции

### Что такое Media Group?

**Media Group** (альбом) - это группа из 2-10 медиа-файлов (фото, видео), которые отправляются пользователю как единое сообщение. В Telegram они отображаются в виде галереи.

**Особенности:**
- Минимум 2, максимум 10 файлов
- Можно группировать фото и видео
- Документы и аудио можно группировать только с файлами того же типа
- Только первый элемент может иметь caption (подпись)

### Зачем это нужно для ИИ?

1. **Результаты обработки** - показать несколько вариантов генерации
2. **Сравнение** - показать "до" и "после" обработки
3. **Галереи** - отправка нескольких результатов работы модели
4. **Пакетная обработка** - получение от пользователя нескольких изображений для обработки

## 🔄 Различия между библиотеками

| Функция | aiogram 3.x | python-telegram-bot 20.x |
|---------|-------------|--------------------------|
| **Отправка альбома** | `bot.send_media_group()` | `bot.send_media_group()` |
| **Построение альбома** | `MediaGroupBuilder` | Список `InputMedia*` |
| **Получение альбома** | Фильтр `F.media_group_id` | Фильтр `filters.PHOTO` + группировка |
| **Группировка** | Встроенная (по `media_group_id`) | Требует middleware/логики |

## 📖 Теория: Отправка альбомов

### aiogram: MediaGroupBuilder

```python
from aiogram.types import FSInputFile, InputMediaPhoto
from aiogram.utils.media_group import MediaGroupBuilder

# Способ 1: MediaGroupBuilder (удобнее)
builder = MediaGroupBuilder(caption="Результаты генерации")
builder.add_photo(media=FSInputFile("image1.jpg"))
builder.add_photo(media=FSInputFile("image2.jpg"))
builder.add_photo(media=FSInputFile("image3.jpg"))

await message.answer_media_group(media=builder.build())

# Способ 2: Список InputMediaPhoto
media = [
    InputMediaPhoto(media=FSInputFile("image1.jpg"), caption="Изображение 1"),
    InputMediaPhoto(media=FSInputFile("image2.jpg")),
    InputMediaPhoto(media=FSInputFile("image3.jpg")),
]
await bot.send_media_group(chat_id=message.chat.id, media=media)
```

### python-telegram-bot: InputMedia список

```python
from telegram import InputMediaPhoto

media = [
    InputMediaPhoto(open("image1.jpg", "rb"), caption="Изображение 1"),
    InputMediaPhoto(open("image2.jpg", "rb")),
    InputMediaPhoto(open("image3.jpg", "rb")),
]

await context.bot.send_media_group(
    chat_id=update.effective_chat.id,
    media=media
)
```

## 📖 Теория: Получение альбомов от пользователя

### Проблема

Когда пользователь отправляет альбом (несколько фото одновременно), бот получает **несколько отдельных Update** с одинаковым `media_group_id`. Их нужно сгруппировать.

### aiogram: Обработка по media_group_id

```python
# Словарь для временного хранения альбомов
albums = {}

@router.message(F.media_group_id, F.photo)
async def handle_album_photo(message: Message):
    media_group_id = message.media_group_id

    # Группируем фото по media_group_id
    if media_group_id not in albums:
        albums[media_group_id] = []

    albums[media_group_id].append(message.photo[-1])

    # Используем asyncio.sleep для ожидания всех фото
    await asyncio.sleep(0.5)

    # Проверяем, что это последнее фото в альбоме
    if len(albums[media_group_id]) >= 2:
        photos = albums[media_group_id]
        await message.answer(f"Получен альбом из {len(photos)} фотографий!")
        del albums[media_group_id]
```

### python-telegram-bot: Группировка в хэндлере

```python
from collections import defaultdict
from telegram.ext import MessageHandler, filters

# Словарь для хранения альбомов
albums = defaultdict(list)

async def handle_album(update: Update, context: ContextTypes.DEFAULT_TYPE):
    message = update.message

    if message.media_group_id:
        # Добавляем фото в альбом
        albums[message.media_group_id].append(message.photo[-1])

        # Ждем 0.5 секунды для сбора всех фото
        await asyncio.sleep(0.5)

        # Обрабатываем альбом
        photos = albums[message.media_group_id]
        if len(photos) >= 2:
            await message.reply_text(
                f"Получен альбом из {len(photos)} фотографий!"
            )
            del albums[message.media_group_id]
```

## 🎨 Практическое применение для ИИ

### 1. Генерация вариантов изображения

```python
# Генерируем 4 варианта с разными seed
from diffusers import StableDiffusionPipeline

variants = []
for seed in range(4):
    image = pipe(prompt, generator=torch.Generator().manual_seed(seed))
    variants.append(image)

# Отправляем альбом
builder = MediaGroupBuilder(caption=f"Варианты для: {prompt}")
for img in variants:
    bio = BytesIO()
    img.save(bio, format='PNG')
    bio.seek(0)
    builder.add_photo(media=BufferedInputFile(bio.read(), "variant.png"))

await message.answer_media_group(media=builder.build())
```

### 2. Сравнение "До и После"

```python
# Обработка изображения (например, улучшение)
original = Image.open("original.jpg")
enhanced = enhance_image(original)  # Ваша функция ИИ

# Отправляем сравнение
media = [
    InputMediaPhoto(media=FSInputFile("original.jpg"), caption="Оригинал"),
    InputMediaPhoto(media=enhanced_bytes, caption="Улучшено ИИ"),
]
await message.answer_media_group(media=media)
```

### 3. Пакетная обработка альбомов

```python
@router.message(F.media_group_id, F.photo)
async def process_album(message: Message, bot: Bot):
    # Собираем альбом (упрощенно)
    photos = await collect_album(message)

    # Обрабатываем все фото
    results = []
    for photo in photos:
        # Скачиваем
        file = await bot.download(photo)

        # Применяем OCR или другую обработку
        text = ocr_model.process(file)
        results.append(text)

    # Отправляем результаты
    full_text = "\n\n".join(results)
    await message.answer(f"Распознанный текст:\n{full_text}")
```

## 🚀 Запуск примеров

### aiogram версия
```bash
export BOT_TOKEN="your_bot_token"
python examples/example_08_media_group_albums/aiogram/bot.py
```

### python-telegram-bot версия
```bash
export BOT_TOKEN="your_bot_token"
python examples/example_08_media_group_albums/python_telegram_bot/bot.py
```

## 📝 Команды бота

- `/start` - Приветствие и инструкции
- `/generate` - Генерация альбома из 3 изображений
- `/compare` - Демонстрация "До и После"
- Отправьте несколько фото одновременно - бот распознает альбом

## 🎓 Что изучили

1. ✅ Отправка медиа-групп (альбомов) ботом
2. ✅ Построение альбомов с помощью MediaGroupBuilder
3. ✅ Получение альбомов от пользователя
4. ✅ Группировка файлов по media_group_id
5. ✅ Практическое применение для ИИ-задач

## 📚 Дополнительные материалы

### Ограничения Media Group:
- Максимум 10 файлов в группе
- Все файлы должны быть одного типа (фото+видео можно, документы отдельно)
- Caption только у первого элемента
- Общий размер файлов не должен превышать 50 МБ

### Лучшие практики:
1. ⭐ **Используйте Middleware** для предотвращения дублирования ответов (см. MIDDLEWARE.md)
2. Используйте `media_group_id` для группировки входящих файлов
3. Добавьте таймаут для сбора всех файлов альбома (0.3-0.5 секунды)
4. Очищайте словарь альбомов после обработки
5. Для больших альбомов используйте очередь обработки

## 🚀 Запуск версии с Middleware (рекомендуется)

### aiogram с middleware:
```bash
cd examples/example_09_media_group_albums/aiogram
export BOT_TOKEN="your_token"
python bot_with_middleware.py
```

### python-telegram-bot с middleware:
```bash
cd examples/example_09_media_group_albums/python_telegram_bot
export BOT_TOKEN="your_token"
python bot_with_middleware.py
```

### Запуск обычной версии (с проблемой дублирования):
```bash
# Только для сравнения!
python bot.py  # Будет дублировать ответы
```

## 🔗 Связанные примеры

- **Example 7** - Загрузка файлов (основа для получения фото)
- **Example 8** - Генерация изображений (основа для создания фото)
- **Example 10** - Telegram Payments (можно продавать альбомы за звезды)

---

**Совет для ИИ-разработчиков:** Альбомы идеально подходят для показа результатов генеративных моделей, которые создают несколько вариантов. Это улучшает UX и позволяет пользователю выбрать лучший результат. **Обязательно используйте Middleware для production ботов!**
