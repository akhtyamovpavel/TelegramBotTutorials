# 🚀 Размещение Telegram WebApp

## ⚠️ Важное ограничение

**Telegram НЕ поддерживает `data:` URLs для WebApp!**

WebApp **обязательно** должен быть размещен на **реальном HTTPS сервере**.

## 🎯 Быстрый старт

### Вариант 1: GitHub Pages (Рекомендуется для учебных целей)

**Преимущества:**
- ✅ Бесплатно
- ✅ Просто
- ✅ Автоматический HTTPS
- ✅ Не требует кредитной карты

**Шаги:**

1. **Создайте репозиторий на GitHub**
   ```bash
   git init
   git add webapp/index.html
   git commit -m "Add WebApp"
   git remote add origin https://github.com/username/telegram-webapp.git
   git push -u origin main
   ```

2. **Включите GitHub Pages**
   - Зайдите в Settings → Pages
   - Source: Deploy from a branch
   - Branch: main / root
   - Сохраните

3. **Получите URL**
   - Ваш WebApp будет доступен по адресу:
   ```
   https://username.github.io/telegram-webapp/webapp/index.html
   ```

4. **Настройте бота**
   ```bash
   export BOT_TOKEN="your_token"
   export WEBAPP_URL="https://username.github.io/telegram-webapp/webapp/index.html"
   python bot.py
   ```

### Вариант 2: Vercel (Быстрое развертывание)

**Преимущества:**
- ✅ Бесплатно
- ✅ Очень быстро
- ✅ Автоматический CI/CD
- ✅ Поддержка custom domains

**Шаги:**

1. **Установите Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **Разверните WebApp**
   ```bash
   cd webapp
   vercel --prod
   ```

3. **Получите URL**
   Vercel выдаст URL вида:
   ```
   https://your-project.vercel.app/index.html
   ```

4. **Настройте бота**
   ```bash
   export WEBAPP_URL="https://your-project.vercel.app/index.html"
   ```

### Вариант 3: Netlify (Drag & Drop)

**Преимущества:**
- ✅ Бесплатно
- ✅ Drag & Drop интерфейс
- ✅ Автоматический HTTPS

**Шаги:**

1. Зайдите на [netlify.com](https://netlify.com)
2. Перетащите папку `webapp` в окно браузера
3. Получите URL:
   ```
   https://random-name.netlify.app/index.html
   ```
4. Настройте бота с этим URL

### Вариант 4: Ngrok (Локальное тестирование)

**Преимущества:**
- ✅ Для разработки и отладки
- ✅ Не требует размещения в интернете
- ⚠️ Временный URL (меняется при перезапуске)

**Шаги:**

1. **Установите Ngrok**
   - Скачайте с [ngrok.com](https://ngrok.com)
   - Или через пакетный менеджер:
   ```bash
   # macOS
   brew install ngrok

   # Ubuntu/Debian
   snap install ngrok
   ```

2. **Запустите локальный сервер**
   ```bash
   cd webapp
   python -m http.server 8000
   ```

3. **Создайте туннель**
   ```bash
   ngrok http 8000
   ```

4. **Получите HTTPS URL**
   Ngrok выдаст URL вида:
   ```
   https://abc123.ngrok.io
   ```

5. **Настройте бота**
   ```bash
   export WEBAPP_URL="https://abc123.ngrok.io/index.html"
   python bot.py
   ```

**⚠️ Важно:** URL от Ngrok меняется при каждом перезапуске! Для постоянного URL нужна платная версия.

### Вариант 5: Свой сервер

**Требования:**
- ✅ Сервер с публичным IP
- ✅ Установленный Nginx/Apache
- ✅ SSL сертификат (Let's Encrypt)

**Пример конфигурации Nginx:**

```nginx
server {
    listen 443 ssl;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    location /webapp/ {
        root /var/www/telegram-bot;
        try_files $uri $uri/ =404;
    }
}
```

## 📁 Структура файлов для размещения

### GitHub Pages / Netlify / Vercel:

```
repository/
├── webapp/
│   └── index.html    # Ваш WebApp
└── README.md
```

### Свой сервер:

```
/var/www/telegram-bot/
└── webapp/
    └── index.html
```

URL будет: `https://your-domain.com/webapp/index.html`

## 🔧 Настройка бота после размещения

### aiogram:

```python
# В начале bot.py
WEBAPP_URL = os.getenv("WEBAPP_URL", "https://your-url.com/index.html")
```

```bash
export BOT_TOKEN="your_bot_token"
export WEBAPP_URL="https://your-url.com/index.html"
python aiogram/bot.py
```

### python-telegram-bot:

```python
# В начале bot.py
WEBAPP_URL = os.getenv("WEBAPP_URL", "https://your-url.com/index.html")
```

```bash
export BOT_TOKEN="your_bot_token"
export WEBAPP_URL="https://your-url.com/index.html"
python python_telegram_bot/bot.py
```

## ✅ Проверка

После размещения проверьте:

1. **Откройте URL в браузере**
   - Должна открыться форма настройки генерации
   - Не должно быть ошибок в консоли браузера

2. **Проверьте HTTPS**
   - URL должен начинаться с `https://`
   - В браузере должен быть зеленый замок 🔒

3. **Тестируйте в боте**
   - Отправьте `/start`
   - Нажмите `/webapp`
   - Нажмите кнопку "⚙️ Открыть настройки генерации"
   - WebApp должен открыться внутри Telegram

## ❓ Часто возникающие проблемы

### ❌ Ошибка "Wrong port number specified in the URL"

**Причина:** Использован `data:` URL или HTTP вместо HTTPS

**Решение:** Разместите WebApp на HTTPS сервере

### ❌ Ошибка "Web App URL is invalid"

**Причина:**
- URL не начинается с `https://`
- Неправильный формат URL
- Сервер недоступен

**Решение:**
- Проверьте, что URL начинается с `https://`
- Откройте URL в браузере - должен открыться WebApp
- Проверьте доступность сервера

### ❌ WebApp открывается, но не отправляет данные

**Причина:** Не подключен Telegram WebApp API

**Решение:** Проверьте, что в HTML есть:
```html
<script src="https://telegram.org/js/telegram-web-app.js"></script>
```

### ❌ CORS ошибки

**Причина:** Telegram делает запросы к WebApp

**Решение:** Убедитесь, что сервер отдает правильные CORS заголовки:
```
Access-Control-Allow-Origin: *
```

## 📚 Дополнительные ресурсы

- [Telegram WebApp документация](https://core.telegram.org/bots/webapps)
- [GitHub Pages документация](https://pages.github.com/)
- [Vercel документация](https://vercel.com/docs)
- [Netlify документация](https://docs.netlify.com/)
- [Ngrok документация](https://ngrok.com/docs)
- [Let's Encrypt](https://letsencrypt.org/)

## 💡 Советы

1. **Для разработки:** Используйте Ngrok
2. **Для учебных проектов:** Используйте GitHub Pages
3. **Для production:** Используйте свой сервер или Vercel/Netlify
4. **Для быстрого прототипа:** Используйте Netlify (drag & drop)

## 🎓 Для студентов

Если вы изучаете создание ботов, рекомендуем:

1. Начать с **GitHub Pages** - это бесплатно и просто
2. Создать публичный репозиторий с вашим WebApp
3. Поделиться ссылкой на репозиторий с преподавателем

**Пример репозитория:**
```
https://github.com/username/telegram-ai-webapp
├── webapp/
│   └── index.html
├── bot/
│   └── bot.py
└── README.md
```
