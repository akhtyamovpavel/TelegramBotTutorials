# 🧪 Тестирование WebApp с отображением параметров

## ✅ Что было улучшено

Теперь бот показывает **красивый структурированный ответ** со всеми полученными параметрами из WebApp.

## 🚀 Быстрое тестирование

### 1. Запустите бота

**Важно:** Сначала разместите WebApp на HTTPS сервере (см. [DEPLOYMENT.md](./DEPLOYMENT.md))

```bash
# Для тестирования используйте Ngrok:
cd webapp
python -m http.server 8000

# В другом терминале:
ngrok http 8000
# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)

# Запустите бота:
cd ..
export BOT_TOKEN="your_token"
export WEBAPP_URL="https://abc123.ngrok.io/index.html"

# aiogram:
python aiogram/bot.py

# или python-telegram-bot:
python python_telegram_bot/bot.py
```

### 2. Тестируйте в Telegram

1. **Отправьте `/start`**
   - Бот покажет приветствие и список команд

2. **Отправьте `/webapp`**
   - Появится кнопка "⚙️ Открыть настройки генерации"

3. **Нажмите на кнопку**
   - Откроется WebApp внутри Telegram

4. **Заполните форму:**
   ```
   Промпт: beautiful sunset over mountains
   Модель: Stable Diffusion XL
   Количество: 2
   Steps: 40
   CFG Scale: 8.5
   Размер: 1024x1024
   ```

5. **Нажмите "🎨 Сгенерировать"**

6. **Проверьте ответ бота:**
   ```
   ✅ Данные успешно получены из WebApp!

   ━━━━━━━━━━━━━━━━━━━━━━
   📋 ПАРАМЕТРЫ ГЕНЕРАЦИИ:
   ━━━━━━━━━━━━━━━━━━━━━━

   📝 Промпт:
      beautiful sunset over mountains

   🤖 Модель: stable-diffusion-xl
   🖼️ Количество изображений: 2
   🔄 Steps (качество): 40
   ⚖️ CFG Scale (точность): 8.5
   📐 Размер: 1024x1024

   ━━━━━━━━━━━━━━━━━━━━━━

   ⏳ Генерирую 2 изображения...
   Пожалуйста, подождите...
   ```

7. **Получите изображения:**
   - Бот отправит 2 placeholder изображения
   - Каждое с подписью "✨ Вариант 1/2" и т.д.

8. **Финальное сообщение:**
   ```
   ✅ Генерация завершена!

   💡 Используйте /webapp для новой генерации с другими параметрами.
   ```

## 🎯 Что проверить

### ✅ Проверка корректности данных

| Параметр | Что проверить | Ожидаемый результат |
|----------|---------------|---------------------|
| **Промпт** | Введите длинный текст (>100 символов) | Весь текст отображается в ответе |
| **Модель** | Выберите разные модели | Правильное название модели в ответе |
| **Количество** | Измените слайдер (1-4) | Правильное количество изображений |
| **Steps** | Измените значение (10-100) | Правильное число в ответе |
| **CFG Scale** | Измените значение (1-20) | Правильное число с десятичной точкой |
| **Размер** | Выберите разные размеры | Правильный размер в ответе |

### ✅ Проверка форматирования

- [ ] Эмодзи отображаются корректно
- [ ] Разделители (━━━) выровнены
- [ ] Промпт в моноширинном шрифте (`<code>`)
- [ ] Жирный текст (`<b>`) работает
- [ ] Переносы строк на месте

### ✅ Проверка placeholder изображений

- [ ] Изображения генерируются
- [ ] На изображении отображается:
  - Название модели
  - Steps и CFG Scale
  - "AI Generated (Demo)"
- [ ] Подпись под изображением корректная

## 🐛 Возможные проблемы

### ❌ Бот не отвечает после нажатия "Сгенерировать"

**Причины:**
- WebApp не может отправить данные
- Бот не зарегистрировал обработчик

**Решение:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Убедитесь, что подключен `telegram-web-app.js`:
   ```html
   <script src="https://telegram.org/js/telegram-web-app.js"></script>
   ```

### ❌ Параметры отображаются неправильно

**Причина:** JSON не парсится

**Решение:**
1. Проверьте логи бота:
   ```
   Получены данные из WebApp от 123456: {"prompt": ...}
   ```
2. Убедитесь, что WebApp отправляет валидный JSON:
   ```javascript
   tg.sendData(JSON.stringify(data));  // ✅ Правильно
   tg.sendData(data);                  // ❌ Неправильно
   ```

### ❌ Изображения не генерируются

**Причина:** Нет библиотеки PIL

**Решение:**
```bash
pip install Pillow
```

### ❌ Форматирование "сломано"

**Причина:** HTML теги не работают

**Решение:**
Убедитесь, что `parse_mode="HTML"` указан:
```python
await message.answer(text, parse_mode="HTML")  # ✅
await message.answer(text)                      # ❌
```

## 📊 Проверка логов

### aiogram:

```bash
# Запустите с подробным логированием:
python aiogram/bot.py

# Ожидаемые логи:
INFO:__main__:Бот запущен и готов к работе с WebApp!
INFO:__main__:WebApp URL: https://abc123.ngrok.io/index.html
INFO:__main__:Получены данные из WebApp от 123456: {"prompt":"...","model":"..."}
INFO:__main__:Отправлено 2 варианта пользователю 123456
```

### python-telegram-bot:

```bash
# Запустите бота:
python python_telegram_bot/bot.py

# Ожидаемые логи:
INFO:__main__:Бот запущен и готов к работе с WebApp!
INFO:__main__:WebApp URL: https://abc123.ngrok.io/index.html
INFO:__main__:Получены данные из WebApp от 123456: {"prompt":"...","model":"..."}
INFO:__main__:Отправлено 2 варианта пользователю 123456
```

## 🔍 Дебаг

### Добавьте дополнительное логирование:

```python
# В handle_webapp_data после получения данных:
logger.info(f"RAW WebApp data: {webapp_data}")
logger.info(f"Parsed settings: {settings}")

# В handle_generation_settings:
logger.info(f"Processing settings: prompt={prompt}, model={model}")
```

### Проверьте данные в WebApp:

```javascript
// В index.html перед sendData():
console.log("Sending data:", JSON.stringify(data));

// После отправки:
tg.MainButton.onClick(() => {
    console.log("Button clicked!");
    sendData();
});
```

## ✅ Чеклист для студентов

- [ ] WebApp размещен на HTTPS сервере
- [ ] Бот запущен с правильным WEBAPP_URL
- [ ] Команда `/webapp` открывает кнопку
- [ ] Кнопка открывает WebApp
- [ ] Форма заполняется
- [ ] Кнопка "Сгенерировать" работает
- [ ] Бот отвечает с параметрами
- [ ] Параметры отображаются правильно
- [ ] Генерируются изображения
- [ ] Финальное сообщение приходит

## 📖 Дополнительные материалы

- **[RESPONSE_EXAMPLE.md](./RESPONSE_EXAMPLE.md)** - пример ответа бота
- **[DEPLOYMENT.md](./DEPLOYMENT.md)** - как разместить WebApp
- **[README.md](./README.md)** - основная документация

## 💡 Советы

1. **Используйте Ngrok для тестирования** - быстро и удобно
2. **Проверяйте логи** - они покажут что происходит
3. **Открывайте консоль браузера** - для отладки JavaScript
4. **Тестируйте с разными параметрами** - убедитесь что все работает
